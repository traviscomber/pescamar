// CANONICAL READ-ONLY QUERIES of api/schema-preflight.ts (source of truth: the endpoint).
// Generated by the endpoint-sql-contracts generator — do not edit by hand.
// scripts/endpoint-sql-contracts.mjs executes these against an ephemeral Neon branch and
// asserts the endpoint source still contains every template, so drift in either direction
// fails CI before deploy. Placeholders (${...}) are runtime values substituted with neutral
// literals for execution: {}

export const META={
 "source": "api/schema-preflight.ts",
 "placeholders": {}
}

export const QUERIES=[
 {name:"runtime_landmarks",template:"select\r\n        to_regclass('public.parties') is not null parties,\r\n        to_regclass('public.receptions') is not null receptions,\r\n        to_regclass('public.operators') is not null operators,\r\n        to_regclass('public.historical_production_records') is not null historical_production_records,\r\n        to_regclass('public.canonical_production_support_blocks') is not null canonical_production_support_blocks,\r\n        to_regclass('public.plant_stations') is not null plant_stations,\r\n        to_regclass('public.packing_units') is not null packing_units,\r\n        to_regclass('public.label_templates') is not null label_templates,\r\n        to_regclass('public.pallets') is not null pallets,\r\n        to_regclass('public.cold_runs') is not null cold_runs,\r\n        to_regclass('public.regulatory_holds') is not null regulatory_holds,\r\n        to_regclass('public.japan_export_release_evidence') is not null japan_export_release_evidence,\r\n        to_regclass('public.lot_lifecycle_events') is not null lot_lifecycle_events,\r\n        to_regclass('public.sea_urchin_external_references') is not null sea_urchin_external_references,\r\n        to_regclass('public.gs1_identity_links') is not null gs1_identity_links",sql:"select\r\n        to_regclass('public.parties') is not null parties,\r\n        to_regclass('public.receptions') is not null receptions,\r\n        to_regclass('public.operators') is not null operators,\r\n        to_regclass('public.historical_production_records') is not null historical_production_records,\r\n        to_regclass('public.canonical_production_support_blocks') is not null canonical_production_support_blocks,\r\n        to_regclass('public.plant_stations') is not null plant_stations,\r\n        to_regclass('public.packing_units') is not null packing_units,\r\n        to_regclass('public.label_templates') is not null label_templates,\r\n        to_regclass('public.pallets') is not null pallets,\r\n        to_regclass('public.cold_runs') is not null cold_runs,\r\n        to_regclass('public.regulatory_holds') is not null regulatory_holds,\r\n        to_regclass('public.japan_export_release_evidence') is not null japan_export_release_evidence,\r\n        to_regclass('public.lot_lifecycle_events') is not null lot_lifecycle_events,\r\n        to_regclass('public.sea_urchin_external_references') is not null sea_urchin_external_references,\r\n        to_regclass('public.gs1_identity_links') is not null gs1_identity_links"},
 {name:"schema_trackers",template:"select schemaname,tablename from pg_tables where schemaname not in ('pg_catalog','information_schema') and (tablename ilike '%migration%' or tablename ilike '%schema%') order by schemaname,tablename",sql:"select schemaname,tablename from pg_tables where schemaname not in ('pg_catalog','information_schema') and (tablename ilike '%migration%' or tablename ilike '%schema%') order by schemaname,tablename"},
 {name:"migration_registry",template:"select migration_name,evidence_kind,applied_at from schema_migrations order by migration_name",sql:"select migration_name,evidence_kind,applied_at from schema_migrations order by migration_name"}
]
