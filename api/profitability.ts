import {requireOperator} from './_auth.js'
import {getSql} from './_db.js'
type Request={method?:string;headers?:Record<string,string|string[]|undefined>}
type Response={status:(code:number)=>Response;setHeader:(name:string,value:string)=>void;json:(body:unknown)=>void}
export default async function handler(req:Request,res:Response){res.setHeader('Cache-Control','no-store');try{const operator=await requireOperator(req);if(!operator)return res.status(401).json({ok:false,error:'Sesión requerida'});if(req.method!=='GET'){res.setHeader('Allow','GET');return res.status(405).json({ok:false,error:'Método no permitido'})}const sql=getSql();const [suppliers,plants,customers,sourceCoverage,historicalSuppliers,historicalPlants,historicalCustomers,historicalProducts,documents,coverage]=await Promise.all([sql`select *,case when revenue_clp>0 then 100*contribution_clp/revenue_clp else null end contribution_pct from supplier_profitability order by contribution_clp desc nulls last`,sql`select * from plant_profitability order by contribution_clp desc nulls last`,sql`select * from customer_profitability order by revenue_clp desc nulls last`,sql`select source_kind,count(*)::int files,min(period_start) period_start,max(period_end) period_end from canonical_source_files where canonical group by source_kind order by source_kind`,sql`select * from historical_supplier_intelligence order by received_kg desc nulls last`,sql`select * from historical_plant_intelligence order by received_kg desc nulls last`,sql`select * from historical_client_intelligence order by source_kg desc nulls last`,sql`select * from historical_product_intelligence order by stock_kg desc nulls last`,sql`select (select count(*)::int from purchase_orders) purchase_orders,(select coalesce(sum(total_amount),0) from purchase_orders where currency='CLP') purchase_orders_clp,(select count(*)::int from export_invoices) export_invoices,(select coalesce(sum(total_amount),0) from export_invoices where currency='USD') export_invoices_usd,(select coalesce(sum(net_weight_kg),0) from export_invoices) export_kg,(select count(*)::int from canonical_packing_boxes) packing_boxes,(select coalesce(sum(total_kg),0) from canonical_packing_boxes) packing_kg`,sql`select (select count(*)::int from historical_production_records where record_status='operational') historical_rows,(select count(*)::int from historical_production_records where record_status='operational' and cardinality(data_quality_flags)>0) historical_flagged,(select count(*)::int from canonical_stock_records) stock_rows,(select count(*)::int from canonical_account_entries) account_rows,(select count(*)::int from canonical_transfers_received) transfer_rows`]);const plantRows=(Array.isArray(plants)?plants:[]).filter((row:any)=>operator.role==='admin'||operator.plantIds.includes(String(row.plant_id??'')));const historicalPlantRows=(Array.isArray(historicalPlants)?historicalPlants:[]).filter((row:any)=>operator.role==='admin'||operator.plantIds.includes(String(row.plant_id??''))||!String(row.plant_id??'').includes('-'));return res.status(200).json({ok:true,live:{suppliers,plants:plantRows,customers},historical:{suppliers:historicalSuppliers,plants:historicalPlantRows,customers:historicalCustomers,products:historicalProducts},documents:Array.isArray(documents)?documents[0]??{}:{},coverage:Array.isArray(coverage)?coverage[0]??{}:{},sourceCoverage})}catch(error){const m=error instanceof Error?error.message:'';return res.status(m.includes('historical_')||m.includes('profitability')?503:500).json({ok:false,error:m.includes('historical_')?'Falta aplicar la migración de inteligencia histórica':m.includes('profitability')?'Falta aplicar la migración de rentabilidad':'No fue posible calcular inteligencia económica'})}}
