import {useMemo,useState} from 'react'

type Lot={reception_id:string;plant_id:string|null;supplier:string;physicalKg:number;purchaseCostClp:number|null}
type Sale={reception_id:string;sold_kg:number|string;price_per_kg_clp:number|string;status:string;customer:string}
type Dimension='supplier'|'plant'|'customer'
const clp=(value:number)=>new Intl.NumberFormat('es-CL',{style:'currency',currency:'CLP',maximumFractionDigits:0}).format(value)
export function ProfitabilityPanel({lots,sales}:{lots:Lot[];sales:Sale[]}){const [dimension,setDimension]=useState<Dimension>('supplier');const rows=useMemo(()=>{const lotById=new Map(lots.map(l=>[l.reception_id,l]));const map=new Map<string,{key:string,revenue:number,cost:number,kg:number,knownKg:number,sales:number}>();for(const sale of sales){if(sale.status!=='confirmed')continue;const lot=lotById.get(sale.reception_id);if(!lot)continue;const key=dimension==='supplier'?lot.supplier:dimension==='plant'?(lot.plant_id??'Sin planta'):(sale.customer||'Cliente no identificado');const soldKg=Number(sale.sold_kg),revenue=soldKg*Number(sale.price_per_kg_clp),unitCost=lot.purchaseCostClp!=null&&lot.physicalKg>0?lot.purchaseCostClp/lot.physicalKg:null;const current=map.get(key)??{key,revenue:0,cost:0,kg:0,knownKg:0,sales:0};current.revenue+=revenue;current.kg+=soldKg;current.sales++;if(unitCost!=null){current.cost+=unitCost*soldKg;current.knownKg+=soldKg}map.set(key,current)}return [...map.values()].map(row=>({...row,margin:row.knownKg>0?row.revenue-row.cost:null,marginPct:row.knownKg>0&&row.revenue>0?((row.revenue-row.cost)/row.revenue)*100:null,coverage:row.kg>0?(row.knownKg/row.kg)*100:0})).sort((a,b)=>b.revenue-a.revenue)},[dimension,lots,sales]);return <section className="panel profitability-panel"><div className="section-heading"><div><span className="overline">Rentabilidad real</span><h2>Resultado consolidado</h2></div><div className="event-kind-tabs" aria-label="Agrupar rentabilidad">{(['supplier','plant','customer'] as Dimension[]).map(value=><button key={value} className={dimension===value?'active':''} onClick={()=>setDimension(value)}>{value==='supplier'?'Proveedor':value==='plant'?'Planta':'Cliente'}</button>)}</div></div><p className="data-caveat">El margen usa únicamente ventas confirmadas y costo de compra proveniente de liquidación. La cobertura indica qué porcentaje de kilos vendidos tiene costo real conocido.</p>{rows.length?<div className="balance-table-wrap"><table className="balance-table profitability-table"><thead><tr><th>{dimension==='supplier'?'Proveedor':dimension==='plant'?'Planta':'Cliente'}</th><th className="numeric">Kg vendidos</th><th className="numeric">Ventas</th><th className="numeric">Costo asignado</th><th className="numeric">Margen</th><th className="numeric">Margen %</th><th className="numeric">Cobertura costo</th></tr></thead><tbody>{rows.slice(0,30).map(row=><tr key={row.key}><td><b>{row.key}</b><small>{row.sales} venta{row.sales===1?'':'s'}</small></td><td className="numeric">{row.kg.toLocaleString('es-CL',{maximumFractionDigits:1})} kg</td><td className="numeric"><b>{clp(row.revenue)}</b></td><td className="numeric">{row.knownKg?clp(row.cost):'—'}</td><td className="numeric"><b className={row.margin!=null&&row.margin<0?'negative':''}>{row.margin==null?'—':clp(row.margin)}</b></td><td className="numeric">{row.marginPct==null?'—':`${row.marginPct.toFixed(1)}%`}</td><td className="numeric"><span className={`status ${row.coverage>=99.9?'success':'warning'}`}>{row.coverage.toFixed(0)}%</span></td></tr>)}</tbody></table></div>:<div className="empty-inline"><div><b>Sin rentabilidad calculable todavía</b><small>Se activará automáticamente cuando existan ventas confirmadas. El margen aparecerá sólo en los kilos que además tengan liquidación de compra.</small></div></div>}</section>}
