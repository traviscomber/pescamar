import {Check,CheckCheck,Clock3,LoaderCircle,MessageSquareText,Scale,X} from 'lucide-react'
import {useEffect,useState} from 'react'
import {PageHeader} from '../components/PageHeader'
import {useAuth} from '../auth'
type Approval={entity_type:'credit_request'|'reception'|'settlement';entity_id:string;reference:string;title:string;detail:string;module:string;owner:string;created_at:string};type ApiResponse={items?:Approval[];error?:string};
async function fetchApprovals(){const response=await fetch('/api/approvals'),data=await response.json() as ApiResponse;if(!response.ok)throw new Error(data.error??'No fue posible consultar las decisiones');return data.items??[]}
export function Approvals(){const {operator}=useAuth(),[items,setItems]=useState<Approval[]>([]),[comments,setComments]=useState<Record<string,string>>({}),[loading,setLoading]=useState(true),[error,setError]=useState(''),[saving,setSaving]=useState('');async function reload(){setLoading(true);try{setItems(await fetchApprovals())}finally{setLoading(false)}}useEffect(()=>{let active=true;fetchApprovals().then(next=>{if(active)setItems(next)}).catch(reason=>{if(active)setError(reason instanceof Error?reason.message:'No fue posible abrir la bandeja')}).finally(()=>{if(active)setLoading(false)});return()=>{active=false}},[]);async function decide(item:Approval,decision:'approved'|'rejected'){const comment=(comments[item.entity_id]??'').trim();if(!comment){setError('Cada decisión requiere un comentario.');return}setSaving(item.entity_id);setError('');try{const response=await fetch('/api/approvals',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({entityType:item.entity_type,entityId:item.entity_id,decision,comment})}),data=await response.json() as ApiResponse;if(!response.ok)throw new Error(data.error??'No fue posible guardar la decisión');setComments(current=>{const next={...current};delete next[item.entity_id];return next});await reload()}catch(reason){setError(reason instanceof Error?reason.message:'No fue posible guardar la decisión')}finally{setSaving('')}}return <><PageHeader eyebrow="Trabajo por excepción" title="Bandeja de decisiones" description={`Cada decisión queda firmada por ${operator?.fullName??'tu sesión'}; la identidad ya no se puede seleccionar manualmente.`}/><section className="decision-toolbar"><div><small>Pendientes</small><b>{items.length}</b></div><p>Sesión activa: <b>{operator?.fullName}</b> · {operator?.role}</p></section>{error?<div className="notice error"><b>Atención.</b> {error}</div>:null}<section className="approval-list">{loading?<div className="panel empty-state"><LoaderCircle className="spin"/><h3>Consultando decisiones</h3></div>:items.length?items.map(item=><article className="panel approval-card approval-card-v2" key={item.entity_id}><div className="approval-symbol"><Scale size={19}/></div><div className="approval-copy"><header><div><span className="overline teal">{item.module} · {item.reference}</span><h2>{item.title}</h2></div><span className="priority high">Pendiente</span></header><p>{item.detail}</p><div className="approval-meta"><span><Clock3 size={13}/>{new Date(item.created_at).toLocaleString('es-CL',{dateStyle:'short',timeStyle:'short'})}</span><span>Solicita: <b>{item.owner}</b></span></div><div className="decision-fields decision-comment"><label><MessageSquareText size={15}/>Comentario obligatorio<input value={comments[item.entity_id]??''} onChange={event=>setComments(current=>({...current,[item.entity_id]:event.target.value}))} placeholder="Fundamento o instrucción"/></label></div><div className="decision-actions"><button className="decision reject" disabled={saving===item.entity_id} onClick={()=>void decide(item,'rejected')}><X size={15}/>Rechazar</button><button className="decision approve" disabled={saving===item.entity_id} onClick={()=>void decide(item,'approved')}><Check size={15}/>{saving===item.entity_id?'Guardando…':'Aprobar'}</button></div></div></article>):<div className="panel empty-state"><CheckCheck size={30}/><h3>No hay decisiones pendientes</h3><p>La bandeja se alimenta únicamente con operaciones reales enviadas a aprobación.</p></div>}</section></>}
