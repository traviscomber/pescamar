import { ArrowRight, CheckCircle2, Factory, Plus, Settings2 } from 'lucide-react'
import { PageHeader } from '../components/PageHeader'
import { productionLines } from '../data'

export function ProductionLines(){return <><PageHeader eyebrow="Motor productivo" title="Líneas de producción" description="Rutas configurables por recurso, formato comercial y mercado de destino." actions={<button className="button primary"><Plus size={16}/>Nueva plantilla</button>}/><section className="line-grid">{productionLines.map(line=><article className="panel line-card" key={line.id}><header><div className="line-icon"><Factory size={20}/></div><div><span className="overline teal">{line.id} · {line.family}</span><h2>{line.name}</h2></div><span className={`status ${line.status==='Activa'?'success':'info'}`}>{line.status}</span></header><div className="process-route" aria-label={`Ruta productiva de ${line.name}`}>{line.route.map((step,index)=><div key={step}><span>{index+1}</span><b>{step}</b>{index<line.route.length-1?<ArrowRight size={13}/>:null}</div>)}</div><dl className="line-specs"><div><dt>Formatos</dt><dd>{line.formats.join(' · ')}</dd></div><div><dt>Control de rendimiento</dt><dd>{line.yieldTarget}</dd></div><div><dt>Mercado</dt><dd>{line.destination}</dd></div></dl><footer><span><CheckCircle2 size={15}/>Trazabilidad y balance de masa incluidos</span><button className="button secondary"><Settings2 size={15}/>Configurar</button></footer></article>)}</section></>}
