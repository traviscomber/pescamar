import {ArrowRight,Bot,Database,LockKeyhole,RefreshCw,Send,ShieldCheck,Sparkles} from 'lucide-react'
import {useEffect,useMemo,useRef,useState,type FormEvent} from 'react'
import {Link,useSearchParams} from 'react-router-dom'
import {useAuth} from '../auth'
import {PageHeader} from '../components/PageHeader'
import {plants} from '../plants'
import {useLots} from '../store'
import '../copilot.css'

type EvidenceClass='live_observation'|'derived_live'|'canonical_reference'|'canonical_history'|'partial_financial'
type Source={id:string;label:string;path:string;rows:number;freshness:string|null;evidenceClass?:EvidenceClass}
type Scope={plantId:string|null;plantIds:string[];role:string;organizationId?:string}
type Turn={id:string;question:string;answer:string;sources:Source[];generatedAt:string;scope:Scope;engine:string;policyVersion:string}
type Payload={answer?:string;engine?:string;policyVersion?:string;generatedAt?:string;scope?:Scope;sources?:Source[];error?:string}
type CanonicalBrief={know:string;meaning:string;action:string;evidencePath:string;priority?:{priority:number;kind:string;title:string;detail:string}|null}
type BriefPayload={ok?:boolean;brief?:CanonicalBrief;error?:string}
const examples=['¿Qué requiere atención hoy y por qué?','¿Qué decisiones puedo tomar con la información histórica?','¿Dónde están las brechas reales de trazabilidad?','¿Qué sabemos con respaldo y qué todavía no debemos afirmar?']
const evidenceLabels:Record<EvidenceClass,string>={live_observation:'actual',derived_live:'calculado',canonical_reference:'fuente de origen',canonical_history:'histórico',partial_financial:'financiero parcial'}

function Answer({text,sources}:{text:string;sources:Source[]}){const lookup=new Map(sources.map(source=>[source.id,source])),parts=text.split(/(\[(?:receptions|production|quality|inventory|orders|canonical_sources|canonical_inventory|historical_lineage|canonical_intelligence|finance|lot_control|operational_intelligence|urchin_graph)\])/g);return <div className="copilot-answer">{parts.map((part,index)=>{const match=/^\[([^\]]+)\]$/.exec(part),source=match?lookup.get(match[1]):null;return source?<Link className="copilot-citation" to={source.path} title={`Abrir ${source.label}`} key={`${part}-${index}`}>{part}</Link>:<span key={index}>{part}</span>})}</div>}

export function Copilot(){
 const {operator}=useAuth(),{lots}=useLots(),[params]=useSearchParams(),available=useMemo(()=>operator?.role==='admin'?plants:plants.filter(plant=>operator?.plantIds.includes(plant.id)),[operator])
 const requestedReceptionId=params.get('receptionId')??''
 const [plantId,setPlantId]=useState(''),[receptionId,setReceptionId]=useState(''),[question,setQuestion]=useState(''),[turns,setTurns]=useState<Turn[]>([]),[loading,setLoading]=useState(false),[error,setError]=useState(''),[brief,setBrief]=useState<CanonicalBrief|null>(null)
 const inputRef=useRef<HTMLTextAreaElement>(null)
 const selectableLots=useMemo(()=>lots.filter(lot=>Boolean(lot.receptionId)&&(!plantId||lot.plantId===plantId)),[lots,plantId])
 useEffect(()=>{if(requestedReceptionId&&selectableLots.some(lot=>lot.receptionId===requestedReceptionId)){setReceptionId(requestedReceptionId);setTurns([])}},[requestedReceptionId,selectableLots])
 useEffect(()=>{if(receptionId&&!selectableLots.some(lot=>lot.receptionId===receptionId)){setReceptionId('');setTurns([])}},[receptionId,selectableLots])
 useEffect(()=>{let active=true;void fetch('/api/canonical-intelligence-brief').then(async response=>{if(!response.ok)return null;return response.json() as Promise<BriefPayload>}).then(payload=>{if(active&&payload?.brief)setBrief(payload.brief)}).catch(()=>undefined);return()=>{active=false}},[])
 async function ask(value=question){const clean=value.trim();if(!clean||loading)return;setLoading(true);setError('');setQuestion('');try{const response=await fetch('/api/copilot',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question:clean,plantId:plantId||null,receptionId:receptionId||null,history:turns.slice(-6).map(turn=>({question:turn.question,answer:turn.answer}))})}),payload=await response.json() as Payload;if(!response.ok||!payload.answer||!payload.generatedAt||!payload.scope)throw new Error(payload.error??'No fue posible obtener una respuesta');setTurns(current=>[...current,{id:crypto.randomUUID(),question:clean,answer:payload.answer!,sources:payload.sources??[],generatedAt:payload.generatedAt!,scope:payload.scope!,engine:payload.engine??'Seafood AI',policyVersion:payload.policyVersion??'seafood.ai.evidence.v6'}])}catch(cause){setError(cause instanceof Error?cause.message:'No fue posible obtener una respuesta')}finally{setLoading(false);requestAnimationFrame(()=>inputRef.current?.focus())}}
 function submit(event:FormEvent){event.preventDefault();void ask()}
 function reset(){setTurns([]);setError('');setQuestion('');inputRef.current?.focus()}
 const plantLabel=plantId?available.find(plant=>plant.id===plantId)?.name??plantId:operator?.role==='admin'?'Toda la red Pescamar':available.length===1?available[0].name:'Todas tus plantas'
 const selectedLot=selectableLots.find(lot=>lot.receptionId===receptionId),selectedIsUrchin=/eriz|urchin/i.test(String(selectedLot?.species??''))
 const selectedPrompts=selectedIsUrchin?['¿Este lote está listo para Japón?','¿Qué lo bloquea ahora?','¿Cuál es la siguiente acción segura?','¿Qué requiere atención y por qué?']:['¿Cuál es el estado de este lote?','¿Qué lo bloquea ahora?','¿Cuál es la siguiente acción segura?','¿Qué requiere atención y por qué?']
 return <><PageHeader eyebrow="Inteligencia y control" title="Seafood AI" description="Asistente de Pescamar para consultar operación e historial. Solo consulta información autorizada y no modifica registros." actions={<><label className="inline-field">Alcance<select value={plantId} onChange={event=>{setPlantId(event.target.value);setReceptionId('');setTurns([])}}><option value="">{operator?.role==='admin'?'Toda la red accesible':'Todas mis plantas'}</option>{available.map(plant=><option value={plant.id} key={plant.id}>{plant.name}</option>)}</select></label><label className="inline-field">Lote<select value={receptionId} onChange={event=>{setReceptionId(event.target.value);setTurns([])}}><option value="">Sin lote específico</option>{selectableLots.map(lot=><option value={lot.receptionId} key={lot.receptionId}>{lot.id} · {lot.species} · {lot.supplier}</option>)}</select></label><button className="button secondary" type="button" onClick={reset}><RefreshCw size={15}/>Nueva conversación</button></>}/><section className="copilot-trust" aria-label="Controles de Seafood AI"><span><Sparkles size={16}/>Basado en registros de Pescamar</span><span><Database size={16}/>{selectedLot?`Lote ${selectedLot.id} + historial relacionado`:'Historial auditado'}</span><span><LockKeyhole size={16}/>{plantLabel}</span><span><ShieldCheck size={16}/>Solo consulta</span></section>{brief&&!plantId&&!receptionId?<section className="canonical-brief" aria-label="Resumen de inteligencia"><div className="canonical-brief-head"><small>Resumen de la red</small><Link to={brief.evidencePath}>Ver respaldo <ArrowRight size={14}/></Link></div><div className="canonical-brief-grid"><div><small>Qué sabemos</small><p>{brief.know}</p></div><div><small>Qué significa</small><p>{brief.meaning}</p></div><div className="canonical-brief-action"><small>Siguiente acción sugerida</small><strong>{brief.action}</strong>{brief.priority?<button type="button" onClick={()=>void ask(`Explícame por qué la prioridad es: ${brief.priority?.title}. Dime qué información la sostiene y cuál es el siguiente paso seguro.`)}>Entender prioridad <ArrowRight size={14}/></button>:null}</div></div></section>:null}<section className="copilot-workspace panel"><div className="copilot-thread" aria-live="polite">{turns.length?turns.map(turn=><article className="copilot-turn" key={turn.id}><div className="copilot-question"><span>Tú</span><p>{turn.question}</p></div><div className="copilot-response"><div className="copilot-avatar"><Bot size={18}/></div><div><div className="copilot-response-heading"><b>Seafood AI</b><small>{new Date(turn.generatedAt).toLocaleString('es-CL')} · {turn.scope.plantId?available.find(plant=>plant.id===turn.scope.plantId)?.name??turn.scope.plantId:'alcance autorizado'}</small></div><Answer text={turn.answer} sources={turn.sources}/>{turn.sources.length?<div className="copilot-sources"><small>Información consultada</small>{turn.sources.map(source=><Link to={source.path} key={source.id}><span>{source.label}{source.evidenceClass?` · ${evidenceLabels[source.evidenceClass]}`:''}</span><b>{source.rows}</b></Link>)}</div>:null}</div></div></article>):<div className="copilot-empty"><span className="copilot-mark"><Sparkles size={24}/></span><h2>{selectedLot?`Pregunta por ${selectedLot.id}`:'Pregunta a la operación'}</h2><p>{selectedLot?(selectedIsUrchin?'Consulta estado, proceso, color, calidad, frío, regulación y preparación para Japón con la información registrada para este lote.':'Consulta estado, bloqueos, balance, respaldo y siguiente acción del lote.'):'Consulta la operación actual junto con el historial de producción, trazabilidad y contexto financiero autorizado.'}</p><div className="copilot-prompts">{(selectedLot?selectedPrompts:examples).map(example=><button type="button" onClick={()=>void ask(example)} key={example}>{example}</button>)}</div></div>}{loading?<div className="copilot-thinking"><span/><span/><span/>Consultando información autorizada…</div>:null}{error?<div className="system-banner error" role="alert">{error}</div>:null}</div><form className="copilot-composer" onSubmit={submit}><label htmlFor="copilot-question" className="sr-only">Pregunta operacional</label><textarea ref={inputRef} id="copilot-question" rows={2} value={question} maxLength={1800} onChange={event=>setQuestion(event.target.value)} onKeyDown={event=>{if(event.key==='Enter'&&!event.shiftKey){event.preventDefault();void ask()}}} placeholder={selectedLot?'Pregunta por estado, bloqueos, balance, respaldo o siguiente acción…':'Pregunta por decisiones, lotes, proveedores, packing, stock, cuentas o historial…'} disabled={loading}/><button className="button primary" type="submit" disabled={loading||!question.trim()}><Send size={16}/><span>Enviar</span></button><small>Enter para enviar · La IA separa hechos de cálculos y no modifica registros.</small></form></section></>
}
