import {AlertTriangle,Boxes,CalendarCheck2,DollarSign,Factory,PackageCheck,RefreshCw,Truck} from 'lucide-react'
import {useEffect,useMemo,useState} from 'react'
import {useAuth} from '../auth'
import {PageHeader} from '../components/PageHeader'
import {plants as configuredPlants} from '../plants'

type Snapshot={date:string;plantId:string|null;generatedAt:string;receptions:{count:number;kg:number};production:{events:number;inputKg:number;outputKg:number;yieldPct:number|null};dispatches:{count:number;kg:number};sales:{count:number;revenueClp:number;knownContributionClp:number;knownContributionSales:number};commitments:{dueOrders:number;dueKg:number;allocatedKg:number;producedCoverageKg:number;allocationCoveragePct:number|null;producedCoveragePct:number|null};pending:{settlements:number;qualityAlerts:number};inventory:{locatedKg:number};transformation:{costClp:number}}
type Payload={snapshot?:Snapshot;saved?:{id:string;generated_by:string;generated_at:string;notes:string|null}|null;permissions?:{canClose:boolean};error?:string}
const kg=(v:number)=>`${v.toLocaleString('es-CL',{maximumFractionDigits:1})} kg`
const clp=(v:number)=>new Intl.NumberFormat('es-CL',{style:'currency',currency:'CLP',maximumFractionDigits:0}).format(v)
const today=()=>new Intl.DateTimeFormat('en-CA',{timeZone:'America/Santiago',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date())
export function DailyClose(){const {operator}=useAuth();const accessiblePlants=useMemo(()=>operator?.role==='admin'?configuredPlants:configuredPlants.filter(plant=>operator?.plantIds.includes(plant.id)),[operator]);const [date,setDate]=useState(today),[plantId,setPlantId]=useState(''),[data,setData]=useState<Payload|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState(''),[notes,setNotes]=useState(''),[busy,setBusy]=useState(false);async function load(nextDate=date,nextPlant=plantId){setLoading(true);try{const q=new URLSearchParams({date:nextDate});if(nextPlant)q.set('plantId',nextPlant);const r=await fetch(`/api/daily-close?${q}`,{cache:'no-store'}),p=await r.json() as Payload;if(!r.ok)throw new Error(p.error??'No fue posible construir el estado del día');setData(p);setNotes(p.saved?.notes??'');setError('')}catch(e){setError(e instanceof Error?e.message:'No fue posible construir el estado del día')}finally{setLoading(false)}}useEffect(()=>{void (async()=>load())()},[]);async function closeDay(){if(!data?.snapshot)return;setBusy(true);setError('');try{const r=await fetch('/api/daily-close',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({date,plantId:plantId||null,notes})}),p=await r.json() as Payload;if(!r.ok)throw new Error(p.error??'No fue posible cerrar el día');await load()}catch(e){setError(e instanceof Error?e.message:'No fue posible cerrar el día')}finally{setBusy(false)}}const s=data?.snapshot;return <><PageHeader eyebrow="Hoy" title="Estado operativo" description="Qué entró, qué se produjo, qué salió, qué se vendió y qué sigue pendiente. Al cerrar el día, esta misma vista queda guardada como snapshot auditable." actions={<div className="page-actions"><label className="inline-field">Fecha<input type="date" value={date} onChange={e=>{setDate(e.target.value);void load(e.target.value,plantId)}}/></label><label className="inline-field">Planta<select value={plantId} onChange={e=>{const next=e.target.value;setPlantId(next);void load(date,next)}}><option value="">Toda la red accesible</option>{accessiblePlants.map(plant=><option key={plant.id} value={plant.id}>{plant.name}</option>)}</select></label><button className="button" onClick={()=>void load()}><RefreshCw size={15}/>Actualizar</button></div>}/>{error?<div className="system-banner error">{error}</div>:null}{loading?<div className="system-banner">Construyendo estado del día desde Neon…</div>:null}{s?<><section className="signal-grid"><article className="signal-card"><span><Boxes size={16}/>Recepción</span><b>{kg(s.receptions.kg)}</b><small>{s.receptions.count} recepciones del día</small></article><article className="signal-card"><span><Factory size={16}/>Producción</span><b>{kg(s.production.outputKg)}</b><small>{s.production.yieldPct==null?'Sin rendimiento del día':`${s.production.yieldPct.toFixed(1)}% rendimiento`}</small></article><article className="signal-card"><span><Truck size={16}/>Despachos</span><b>{kg(s.dispatches.kg)}</b><small>{s.dispatches.count} salidas confirmadas</small></article></section><section className="close-grid"><article className="panel"><div className="section-heading"><div><span className="overline">Resultado del día</span><h2>Flujo físico y contribución</h2></div></div><div className="balance-summary"><div><small>Ventas</small><b>{clp(s.sales.revenueClp)}</b></div><div><small>Contribución conocida</small><b>{s.sales.knownContributionSales?clp(s.sales.knownContributionClp):'—'}</b></div><div><small>Inventario ubicado</small><b>{kg(s.inventory.locatedKg)}</b></div></div><div className="close-lines"><p><PackageCheck size={15}/><span>Producción entrada</span><b>{kg(s.production.inputKg)}</b></p><p><PackageCheck size={15}/><span>Producción salida</span><b>{kg(s.production.outputKg)}</b></p><p><DollarSign size={15}/><span>Costos transformación registrados</span><b>{clp(s.transformation.costClp)}</b></p><p><DollarSign size={15}/><span>Ventas confirmadas</span><b>{s.sales.count}</b></p></div></article><article className="panel"><div className="section-heading"><div><span className="overline">Lo que requiere acción</span><h2>Compromisos y pendientes</h2></div></div><div className="close-lines"><p className={s.commitments.dueOrders?'attention':''}><CalendarCheck2 size={15}/><span>Órdenes vencidas / por entregar</span><b>{s.commitments.dueOrders} · {kg(s.commitments.dueKg)}</b></p><p className={s.commitments.dueKg>0&&Number(s.commitments.allocationCoveragePct??0)<100?'attention':''}><PackageCheck size={15}/><span>Cobertura reservada</span><b>{kg(s.commitments.allocatedKg)} · {s.commitments.allocationCoveragePct==null?'—':`${s.commitments.allocationCoveragePct.toFixed(0)}%`}</b></p><p className={s.commitments.dueKg>0&&Number(s.commitments.producedCoveragePct??0)<100?'attention':''}><Factory size={15}/><span>Cobertura producida confirmada</span><b>{kg(s.commitments.producedCoverageKg)} · {s.commitments.producedCoveragePct==null?'—':`${s.commitments.producedCoveragePct.toFixed(0)}%`}</b></p><p className={s.pending.qualityAlerts?'attention':''}><AlertTriangle size={15}/><span>Alertas de calidad abiertas</span><b>{s.pending.qualityAlerts}</b></p><p className={s.pending.settlements?'attention':''}><DollarSign size={15}/><span>Liquidaciones pendientes</span><b>{s.pending.settlements}</b></p></div></article></section><section className="panel daily-close-action"><div><span className="overline">Cierre auditable</span><h2>{data?.saved?'Día cerrado':s.receptions.count||s.production.events||s.dispatches.count||s.sales.count?'Cerrar esta jornada':'Día sin operación registrada'}</h2><p>{data?.saved?`Snapshot guardado por ${data.saved.generated_by}. Si corriges información del mismo día puedes regenerarlo manteniendo trazabilidad.`:s.receptions.count||s.production.events||s.dispatches.count||s.sales.count?'Cuando la jornada termine, congela exactamente este estado para compararlo con los días siguientes.':'No hay actividad operacional para esta fecha y alcance.'}</p></div>{data?.permissions?.canClose&&(s.receptions.count||s.production.events||s.dispatches.count||s.sales.count)?<div className="close-form"><textarea rows={3} value={notes} onChange={e=>setNotes(e.target.value)} placeholder="Observaciones del cierre"/><button className="button primary" onClick={()=>void closeDay()} disabled={busy}><CalendarCheck2 size={15}/>{busy?'Guardando…':data.saved?'Regenerar snapshot':'Cerrar jornada'}</button></div>:null}</section></>:null}</>}
